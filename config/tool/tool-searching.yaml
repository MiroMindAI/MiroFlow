name: "tool-searching"
tool_command: "python"
args:
  - "-m"
  - "src.tool.mcp_servers.search_and_scrape_webpage"
env:
# Search API keys - these values will be loaded from the .env file at runtime
  SERPER_API_KEY: "${oc.env:SERPER_API_KEY}"