defaults:
  - benchmark: example_dataset
  - override hydra/job_logging: none
  - _self_

main_agent:
  prompt_class: MainAgentPromptBoxedAnswer
  llm:
    provider_class: "OpenCodeClient"
    model_name: "anthropic/claude-opus-4-5"
    async_client: true
    temperature: 0.3
    top_p: 0.95
    min_p: 0.0
    top_k: -1
    max_tokens: 32000
    opencode_path: "${oc.env:OPENCODE_PATH,opencode}"
    keep_tool_result: -1
    oai_tool_thinking: false

  tool_config:
    - tool-reasoning
    - tool-context7

  max_turns: 30
  max_tool_calls_per_turn: 10

  input_process:
    hint_generation: false
    hint_llm_base_url: "${oc.env:HINT_LLM_BASE_URL,https://api.openai.com/v1}"
  output_process:
    final_answer_extraction: false
    final_answer_llm_base_url: "${oc.env:FINAL_ANSWER_LLM_BASE_URL,https://api.openai.com/v1}"

  openai_api_key: "${oc.env:OPENAI_API_KEY,dummy}"
  add_message_id: true
  keep_tool_result: -1
  chinese_context: "${oc.env:CHINESE_CONTEXT,false}"

sub_agents:
  agent-code-review:
    prompt_class: SubAgentWorkerPrompt
    llm:
      provider_class: "OpenCodeClient"
      model_name: "anthropic/claude-opus-4-5"
      async_client: true
      temperature: 0.2
      top_p: 0.95
      min_p: 0.0
      top_k: -1
      max_tokens: 32000
      opencode_path: "${oc.env:OPENCODE_PATH,opencode}"
      keep_tool_result: -1
      oai_tool_thinking: false

    tool_config:
      - tool-reading
      - tool-context7
      - tool-code

    max_turns: 20
    max_tool_calls_per_turn: 10

  agent-pixel-perfect:
    prompt_class: SubAgentWorkerPrompt
    llm:
      provider_class: "OpenCodeClient"
      model_name: "anthropic/claude-opus-4-5"
      async_client: true
      temperature: 0.2
      top_p: 0.95
      min_p: 0.0
      top_k: -1
      max_tokens: 32000
      opencode_path: "${oc.env:OPENCODE_PATH,opencode}"
      keep_tool_result: -1
      oai_tool_thinking: false

    tool_config:
      - tool-reading
      - tool-image-video
      - tool-talk-to-figma

    max_turns: 15
    max_tool_calls_per_turn: 10

  agent-ios-developer:
    prompt_class: SubAgentWorkerPrompt
    llm:
      provider_class: "OpenCodeClient"
      model_name: "anthropic/claude-opus-4-5"
      async_client: true
      temperature: 0.3
      top_p: 0.95
      min_p: 0.0
      top_k: -1
      max_tokens: 32000
      opencode_path: "${oc.env:OPENCODE_PATH,opencode}"
      keep_tool_result: -1
      oai_tool_thinking: false

    tool_config:
      - tool-reading
      - tool-context7
      - tool-code

    max_turns: 25
    max_tool_calls_per_turn: 10

  agent-rust-developer:
    prompt_class: SubAgentWorkerPrompt
    llm:
      provider_class: "OpenCodeClient"
      model_name: "anthropic/claude-opus-4-5"
      async_client: true
      temperature: 0.3
      top_p: 0.95
      min_p: 0.0
      top_k: -1
      max_tokens: 32000
      opencode_path: "${oc.env:OPENCODE_PATH,opencode}"
      keep_tool_result: -1
      oai_tool_thinking: false

    tool_config:
      - tool-reading
      - tool-context7
      - tool-code

    max_turns: 25
    max_tool_calls_per_turn: 10

  agent-researcher:
    prompt_class: SubAgentWorkerPrompt
    llm:
      provider_class: "OpenCodeClient"
      model_name: "anthropic/claude-opus-4-5"
      async_client: true
      temperature: 0.4
      top_p: 0.95
      min_p: 0.0
      top_k: -1
      max_tokens: 32000
      opencode_path: "${oc.env:OPENCODE_PATH,opencode}"
      keep_tool_result: -1
      oai_tool_thinking: false

    tool_config:
      - tool-reading
      - tool-context7
      - tool-playwright

    max_turns: 30
    max_tool_calls_per_turn: 10

output_dir: logs/
data_dir: "${oc.env:DATA_DIR,data}"
